# fetch APi -- APPLICATION PROGRAMMING INTERFACE
- api is built by another person we just give request to fetch data from api
- this api might store recipie of food, weather data or something else
- this is `request-response cycle`
- when we request for data, api gives us response

- api data kahan se lata wo data thek hy ya ni does not matter, bs hmen response milna chaye
- in api call, page is not reload and every changes occur at runtime

# Fetch api provides an interface for fetching(sending/receiving) resources
- api works on request and response objects
- fetch gives us promise
- api takes some data to fetch data so it gives us promise first
- so we use await till api give data

- fetch gives first promise
- json gives secocnd promise

# Fetch method
- when we use fetch method and does not pass option then fetch api give special type of request object called get request

# Understanding terms
- AJAX -- asynchronous js and xml (data format)  OLD version
- JSON -- nowadays data format is json object also called AJAJ

# AJAX (Xml) ----> AJAJ (Json)

1. Response (Json)
2. Convert into json()

- Input(JSON) --> Output(json)


# GET Request --- VERY IMP
- used to get/retrieve some data

# POST Request
- used to send data

# DELETE Request
- deactivate your account

# UPDATE Request
- patch request used to update your username

# HTTP Response status code
- 200 --- successful
- 404 --- page not found
- 500 --- api/server gives error

# Headers
- contain extra information in request-response cycle
